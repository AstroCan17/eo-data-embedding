# Core ML
torch>=2.1
torchvision>=0.16
timm>=1.0.0          # ViT backbones for Phase 0 sanity + baselines

# Geospatial / EO
torchgeo>=0.6        # datasets (EuroSAT, BigEarthNet, OSCD) + pretrained weights + samplers
webdataset>=0.2      # streaming loaders (e.g. SSL4EO-S12 cross-modal)
# SSL4EO-S12 v1.1 streaming loader deps (the loader itself is cloned in the Dockerfile):
zarr==2.18.0
numcodecs==0.15.1
xarray
braceexpand
albumentations
rasterio>=1.3
# stackstac>=0.5     # uncomment for raw Sentinel pulls via Planetary Computer
# pystac-client>=0.7

# Embeddings / search
faiss-cpu>=1.7       # swap for faiss-gpu on the P40/Kaggle if desired
pandas>=2.0
pyarrow>=14.0        # parquet embedding store
numpy>=1.24
scikit-learn>=1.3    # linear probe, metrics

# Demo / app
gradio>=4.0

# Utils
pyyaml>=6.0

# Foundation models (Phase 1+):
#   `claymodel` is baked into the GPU Dockerfile (needs Python >=3.11) — not here, so the
#   lightweight CPU image stays small. Only the checkpoint must be fetched once:
#       huggingface-cli download made-with-clay/Clay clay-v1.5.ckpt --local-dir .
#   Prithvi (optical fallback): huggingface ibm-nasa-geospatial/Prithvi-EO-2.0-300M
# See research/04-clay-integration.md.
